This repo runs on two Adafruit Feather M0 board run off of Arduino IDE. The boards require a BMP 280 sensor attached to the board.

The DEBUG should be set to 0 if the board if not connected to the USB port of a computer.


Need to add DEBUG logic to all, 
Add led flash for status, 
Add local storage of data
